/** @deprecated Use `@/lib/player-nav` — `/mobile` redirects to `/player`. */
export {
  getActivePlayerNavItem as getActiveMobileNavItem,
  playerRoutes as mobileRoutes,
  type PlayerNavItem as MobileNavItem,
} from "@/lib/player-nav";
